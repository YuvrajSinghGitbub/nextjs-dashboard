// The `page` file holds special meaning
// Each seprate folder inside root represents a url endpoint if it contains `page` file
export default function Page() {
    return <p>This is the dashboard</p>
}
